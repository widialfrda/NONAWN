<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nonawn">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User.Profile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Common.SignUpLogin.SuksesForgetPassMsg" />
        <activity android:name=".Common.SignUpLogin.SetNewPass" />
        <activity android:name=".Common.SignUpLogin.PilihanReset" />
        <activity android:name=".Common.SignUpLogin.ForgetPassword" />
        <activity android:name=".Common.SignUpLogin.OTPVerify" />
        <activity android:name=".Common.SignUpLogin.Signup" />
        <activity android:name=".Common.SignUpLogin.Login" />
        <activity android:name=".Common.SignUpLogin.RetailerWelcomeScreen" />
        <activity android:name=".User.Tentang" />
        <activity android:name=".Common.OnBoarding" />
        <activity android:name=".User.UserDashboard" />
        <activity android:name=".User.ReviewProduk2" />
        <activity android:name=".Common.splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>